<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#EEEEEE" >

    <ListView
        android:id="@+id/fragment_view_conversation_message_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/fragment_view_conversation_text_entry"
        android:animateLayoutChanges="false"
        android:cacheColorHint="#00000000"
        android:divider="@null"
        android:dividerHeight="0px"
        android:scrollingCache="true"
        android:stackFromBottom="true"
        android:isScrollContainer="true" >
    </ListView>

    <EditText
        android:id="@+id/fragment_view_conversation_text_entry"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/fragment_view_conversation_send_message"
        android:inputType="textCapSentences|textMultiLine"
        android:isScrollContainer="true"
        android:maxHeight="200dp"
        android:minHeight="60dp" >
    </EditText>

    <Button
        android:id="@+id/fragment_view_conversation_send_message"
        android:layout_width="80dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginTop="5dp"
        android:text="Send" />

</RelativeLayout>